[pytest]
# Pytest configuration for chat_to project

# Test discovery
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = tests

# Markers
markers =
    unit: Unit tests (isolated, fast)
    integration: Integration tests (multiple components)
    e2e: End-to-end tests (full user journeys)
    slow: Tests that take more than 5 seconds
    requires_ollama: Tests requiring Ollama LLM to be running
    asyncio: Async tests (automatically handled by pytest-asyncio)
    group_chat_unit: Unit tests for group chat service
    group_chat_integration: Integration tests for group chat
    group_chat_e2e: E2E tests for group chat
    sliding_window: Tests for message window functionality
    partial_failure: Tests for partial failure scenarios

# Output options
addopts =
    -v
    --tb=short
    --strict-markers
    --disable-warnings
    -ra

# Async support
asyncio_mode = auto

# Coverage options (when using --cov)
[coverage:run]
source = backend
omit =
    */venv/*
    */tests/*
    */__pycache__/*
    */site-packages/*

[coverage:report]
precision = 2
show_missing = True
skip_covered = False

[coverage:html]
directory = htmlcov
